# import * from './generated/prisma.graphql';

type Query {
  items: [Item]
}

type Mutation {
  createItem(
    title: String!
    description: String!
    image: String
    largeImage: String
    price: Int!
  ): Item!
}
